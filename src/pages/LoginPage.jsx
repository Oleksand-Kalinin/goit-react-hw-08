import LoginForm from "../components/LoginForm/LoginForm";

function LoginPage() {
  return (
    <section
      style={{
        padding: "20px",
      }}
    >
      <LoginForm />
    </section>
  );
}

export default LoginPage;
